{
  "name": "bnijenhuis-nl",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch:sass": "sass --watch css/sass:css --style compressed",
    "build:sass": "sass css/sass:css --style compressed",
    "watch:eleventy": "npx eleventy --serve",
    "build:eleventy": "npx eleventy",
    "start": "npm-run-all build:sass --parallel watch:*",
    "build": "npm-run-all build:*",
    "debug": "DEBUG=* npx eleventy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1",
    "@11ty/eleventy-plugin-rss": "^1.1.1"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5",
    "sass": "^1.32.8"
  }
}
