---
layout: layout.liquid
---

<main>

    <h1>{{ pageTitle }}</h1>

    <p>Posted on: <time datetime="{{ page.date | postDate }}">{{ page.date | readablePostDate }}</time></p>

    {{ content }}

</main>

{% include 'webmentions' %}
